[project]
name = "omnicorp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "bioc>=2.1",
    "click>=8.1.8",
    "duckdb>=1.1.3",
]

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

[project.scripts]
pubtator3 = "pubtator3.pubtator3_loader:pubtator3_loader"
